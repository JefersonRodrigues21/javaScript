// * Operador Ternário* - Uma forma mais curta de escrever condicionais simples

const idade = 15;

// idade >= 18 ? console.log('Maior de idade') : console.log('Menor de idade');

const mensagem = idade >= 18 ? 'Maior de idade' : 'Menor de idade'; // ? = if, e : = else

console.log(mensagem);
